pipeline {
    agent any

    stages {
        stage('build image') {
            steps {
                sh '''
                docker rm -f $(docker ps -aq)
                docker build -t adele:001
                docker images
                    
                '''
            }
        }


    }



}
