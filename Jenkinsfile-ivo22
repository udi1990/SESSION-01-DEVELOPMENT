pipeline {
    agent any
    options {
    buildDiscarder(logRotator(numToKeepStr: '30', artifactNumToKeepStr: '30')) }

    stages {
        stage('Hello1') {
            steps {
                sh'''
                ls
                pwd
                touch ivo
                '''
            }
        }

        stage('Hello2') {
            steps {
                sh'''
                mkdir tom
                free -h
                '''
            }
        }

        stage('Hello3') {
            steps {
                sh'''
                uname -r
                '''
            }
        }
    }
}
